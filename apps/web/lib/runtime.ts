const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:3001'; export const RuntimeFenceAPI = { async deploy(account: string) { const res = await fetch(API_URL + '/api/v1/runtime/deploy', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ owner: account }) }); return res.json(); }, async kill(instanceId: string) { const res = await fetch(API_URL + '/api/v1/killswitch/trigger', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify({ instanceId, trigger: 'manual' }) }); return res.json(); }, async getStatus(instanceId: string) { const res = await fetch(API_URL + '/api/v1/runtime/' + instanceId + '/status'); return res.json(); }, async assess(tx: any) { const res = await fetch(API_URL + '/api/v1/runtime/assess', { method: 'POST', headers: { 'Content-Type': 'application/json' }, body: JSON.stringify(tx) }); return res.json(); } };
